import { useEffect,useState } from "react";import { approveOrder,getAdminOrders,rejectOrder } from "../../api/adminApi";
export default function AdminOrders(){const [rows,setRows]=useState<any[]>([]);const [msg,setMsg]=useState("");const load=()=>getAdminOrders().then(r=>setRows(r.data));useEffect(()=>{load()},[]);const approve=async(id:string)=>{try{const r=await approveOrder(id);setMsg(r.data.message);load()}catch(e:any){setMsg(e?.response?.data?.message||"Failed")}};const reject=async(id:string)=>{await rejectOrder(id);load()};return <div><h1>Home Orders</h1>{msg&&<p className="success">{msg}</p>}<div className="cards">{rows.map(o=><div className="productCard" key={o._id}><h3>{o.userId?.name}</h3><p>{o.userId?.email} · {o.userId?.phone}</p><p>Status: <b>{o.status}</b></p><p>Total: ₹{o.totalAmount}</p>{o.items.map((i:any)=><p key={i.homeItemId}>{i.title} · {i.itemType}</p>)}<div className="row"><button className="primary" onClick={()=>approve(o._id)}>Approve + QR</button><button onClick={()=>reject(o._id)}>Reject</button></div></div>)}</div></div>}
