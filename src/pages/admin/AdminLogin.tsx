import { useState } from "react";import { useNavigate } from "react-router-dom";import { loginAdmin } from "../../api/authApi";import { saveSession } from "../../store/auth";
export default function AdminLogin(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [msg,setMsg]=useState("");const nav=useNavigate();const submit=async(e:React.FormEvent)=>{e.preventDefault();try{const {data}=await loginAdmin(email,password);saveSession(data.token,"admin");nav("/admin/dashboard")}catch(err:any){setMsg(err?.response?.data?.message||"Login failed")}};return <main className="loginPage"><form className="panel" onSubmit={submit}><h1>Admin Login</h1><input type="email" placeholder="Admin email" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="primary">Login</button>{msg&&<p>{msg}</p>}</form></main>}
