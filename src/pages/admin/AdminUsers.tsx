import { useEffect,useState } from "react";import { getAdminUsers, updateUserStatus } from "../../api/adminApi";
export default function AdminUsers(){const [rows,setRows]=useState<any[]>([]);const load=()=>getAdminUsers().then(r=>setRows(r.data));useEffect(()=>{load()},[]);const setStatus=async(id:string,status:string)=>{await updateUserStatus(id,status);load()};return <div><h1>Users</h1><div className="tableWrap"><table><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Status</th><th>Actions</th></tr></thead><tbody>{rows.map(u=><tr key={u._id}><td>{u.name}</td><td>{u.email}</td><td>{u.phone}</td><td>{u.status}</td><td><button onClick={()=>setStatus(u._id,"approved")}>Approve</button><button onClick={()=>setStatus(u._id,"rejected")}>Reject</button></td></tr>)}</tbody></table></div></div>}
